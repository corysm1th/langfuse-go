// Code generated by Fern. DO NOT EDIT.

package ingestion_test

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	http "net/http"
	os "os"
	testing "testing"

	langfuse "github.com/corysm1th/langfuse-go"
	client "github.com/corysm1th/langfuse-go/client"
	option "github.com/corysm1th/langfuse-go/option"
	require "github.com/stretchr/testify/require"
)

func VerifyRequestCount(
	t *testing.T,
	testId string,
	method string,
	urlPath string,
	queryParams map[string]any,
	expected int,
) {
	wiremockURL := os.Getenv("WIREMOCK_URL")
	if wiremockURL == "" {
		wiremockURL = "http://localhost:8080"
	}
	WiremockAdminURL := wiremockURL + "/__admin"
	var reqBody bytes.Buffer
	reqBody.WriteString(`{"method":"`)
	reqBody.WriteString(method)
	reqBody.WriteString(`","urlPath":"`)
	reqBody.WriteString(urlPath)
	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
	reqBody.WriteString(testId)
	reqBody.WriteString(`"}}`)
	if len(queryParams) > 0 {
		reqBody.WriteString(`,"queryParameters":{`)
		first := true
		for key, value := range queryParams {
			if !first {
				reqBody.WriteString(",")
			}
			reqBody.WriteString(`"`)
			reqBody.WriteString(key)
			switch v := value.(type) {
			case string:
				reqBody.WriteString(`":{"equalTo":"`)
				reqBody.WriteString(v)
				reqBody.WriteString(`"}`)
			case []string:
				reqBody.WriteString(`":{"hasExactly":[`)
				for i, item := range v {
					if i > 0 {
						reqBody.WriteString(",")
					}
					reqBody.WriteString(`{"equalTo":"`)
					reqBody.WriteString(item)
					reqBody.WriteString(`"}`)
				}
				reqBody.WriteString(`]}`)
			}
			first = false
		}
		reqBody.WriteString("}")
	}
	reqBody.WriteString("}")
	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
	require.NoError(t, err)
	var result struct {
		Requests []interface{} `json:"requests"`
	}
	json.NewDecoder(resp.Body).Decode(&result)
	require.Equal(t, expected, len(result.Requests))
}

func TestIngestionBatchWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewLangfuseClient(
		option.WithBaseURL(WireMockBaseURL),
		option.WithBasicAuth("test-username", "test-password"),
	)
	request := &langfuse.IngestionBatchRequest{
		Batch: []*langfuse.IngestionEvent{
			&langfuse.IngestionEvent{
				IngestionEventZero: &langfuse.IngestionEventZero{
					ID:        "abcdef-1234-5678-90ab",
					Timestamp: "2022-01-01T00:00:00.000Z",
					Body: &langfuse.TraceBody{
						ID: langfuse.String(
							"abcdef-1234-5678-90ab",
						),
						Timestamp: langfuse.Time(
							langfuse.MustParseDateTime(
								"2022-01-01T00:00:00Z",
							),
						),
						Name: langfuse.String(
							"My Trace",
						),
						UserID: langfuse.String(
							"1234-5678-90ab-cdef",
						),
						Input:  "My input",
						Output: "My output",
						SessionID: langfuse.String(
							"1234-5678-90ab-cdef",
						),
						Release: langfuse.String(
							"1.0.0",
						),
						Version: langfuse.String(
							"1.0.0",
						),
						Metadata: "My metadata",
						Tags: []string{
							"tag1",
							"tag2",
						},
						Environment: langfuse.String(
							"production",
						),
						Public: langfuse.Bool(
							true,
						),
					},
					Type: langfuse.IngestionEventZeroTypeTraceCreate,
				},
			},
		},
	}
	invocationErr := client.Ingestion.Batch(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestIngestionBatchWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestIngestionBatchWithWireMock", "POST", "/api/public/ingestion", nil, 1)
}
